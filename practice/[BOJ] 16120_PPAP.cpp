#include <iostream>
#include <cstring>
using namespace std;
char ppap[1000001];
bool check[1000001];
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie();
	cin >> ppap;
	int len = strlen(ppap);
	for (int i=0; i<len-3; i++) {
	}
	return 0;
}
