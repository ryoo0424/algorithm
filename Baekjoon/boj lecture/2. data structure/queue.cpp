#include<iostream>
#include<queue>
#include<stack>
#include<string> //stoi, stol, stoll... 문자열->수 / to_string() 수->문자열 
using namespace std;
int main(void){
	return 0;
}
/*
deck: 양 끝에서 push, pop이 가능한 자료구조 
문자열 길이 재기
for (int i=0; s[i]; i++) len += 1;
문자열의 끝은 NULL == 0 이기 때문에 이렇게 할 수 있음 
*/
